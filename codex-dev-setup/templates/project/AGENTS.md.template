# Project environment

{{ENVIRONMENT_RULES}}

# Project commands

{{PROJECT_COMMANDS}}

# Working agreements

- Use only the declared commands and the repository's lockfile and package manager.
- Preserve unrelated changes and keep edits scoped to the request.
- Diagnose the first command failure before changing shell syntax or toolchains.
- Ask before adding production dependencies or changing public interfaces.
- Report checks run, results, skipped checks, and remaining risks.
